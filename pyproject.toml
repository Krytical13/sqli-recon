[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sqli-recon"
version = "1.0.0"
description = "Active SQL injection surface discovery tool"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.28.0",
    "requests[socks]>=2.28.0",
    "beautifulsoup4>=4.11.0",
    "lxml>=4.9.0",
    "urllib3>=1.26.0",
]

[project.optional-dependencies]
headless = ["playwright>=1.40.0"]
bench = ["pyyaml>=6.0"]

[project.scripts]
sqli-recon = "sqli_recon.cli:main"
recon-gather = "sqli_recon.gather:main"
infra-map = "infra_map.cli:main"

[tool.setuptools.packages.find]
# bench/ is a dev-only accuracy harness — not included in the distribution wheel
include = ["sqli_recon*", "infra_map*"]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "--import-mode=importlib"
